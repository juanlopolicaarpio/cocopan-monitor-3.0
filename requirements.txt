# Core Dependencies for Production Monitor
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# Async HTTP and Concurrency
aiohttp>=3.9.0
asyncio-throttle>=1.0.0
nest-asyncio>=1.5.6

# Browser Automation (headless)
playwright>=1.40.0

# Dashboard and Visualization
streamlit>=1.28.0
plotly>=5.17.0
pandas>=2.2.0

# Enhanced Authentication (NEW - REQUIRED for persistent login)
streamlit-cookies-manager>=0.2.0

# Time and Timezone
pytz>=2024.1

# Docker client (optional helper)
docker==7.1.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Scheduling (optional)
APScheduler>=3.10.0

# System Monitoring and Health
psutil>=5.9.0

# Resilience / Retries
tenacity>=8.2.0

# Logging
structlog>=23.1.0

# Performance (Linux only)
uvloop>=0.19.0; sys_platform == "linux"

# Testing (dev)
pytest>=7.4.0
pytest-asyncio>=0.21.0
fuzzywuzzy[speedup]
# Add these lines to your existing requirements.txt

# --- Browser Automation ---
selenium>=4.24.0          # required because your code imports selenium.*
webdriver-manager>=4.0.0  # optional, helps auto-manage Chrome drivers

# --- HTML Parsing (recommended for BeautifulSoup) ---
lxml>=5.2.0               # faster/more reliable than default parser

